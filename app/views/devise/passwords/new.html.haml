%div.sign-up-wrapp
  .form-signup.col-sm-6
    %h2 Forgotten your password?
    %p.sub-heading We can help you recover your password.
    = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
      = f.error_notification
      .form-inputs
        = f.input_field :email, required: true, autofocus: true, :placeholder => "EMAIL"
      .form-actions
        = f.button :submit, "Send me password reset instructions"
        = link_to "< BACK", :back, class: 'password-new-back back-form'
